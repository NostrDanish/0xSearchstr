# 0xSearchstr Content Hash Blocklist
# ─────────────────────────────────────────────────────────────────
# One SHA-256 hash per line (lowercase hex).
# Lines starting with # are comments.
#
# These are SHA-256 hashes of known illegal content (CSAM images,
# trafficking listings, etc.). When the crawler encounters content
# whose hash matches an entry here, it is immediately dropped.
#
# Sources for hash lists:
# - PhotoDNA / Microsoft hash sharing
# - IWF hash list
# - NCMEC hash list
# - StopCSAM collaborative hash sets
#
# IMPORTANT: This file ships empty. Populate from established
# hash-sharing programs before production deployment.
#
# Format: one SHA-256 hex hash per line (64 characters)
# Example:
# e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855
