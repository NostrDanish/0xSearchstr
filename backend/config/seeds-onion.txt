# 0xSearchstr Tor (.onion) Seed URLs
# ─────────────────────────────────────────────────────────────────
# One .onion URL per line. Lines starting with # are comments.
#
# IMPORTANT: Only add legitimate services here.
# All crawled content passes through the content policy pipeline
# before indexing. Marketplaces and illegal services are filtered out.
#
# Recommended sources for seed URLs:
# - Ahmia's search index (https://ahmia.fi)
# - The Hidden Wiki (curated, vetted entries only)
# - SecureDrop directory (https://securedrop.org/directory/)
#
# Example entries (replace with real .onion addresses):
# http://2gzyxa5ihm7nsber.onion  (Tor Project)
# http://sdolvtfhatvsysc6l34d65ymdwxcujausez7raosdec6p7q7z7l5.onion (SecureDrop)
